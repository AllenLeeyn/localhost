# localhost/Cargo.toml

[package]
name = "localhost"
version = "0.1.0"
edition = "2021"

[dependencies]
libc = "0.2.177"
mime_guess = "2.0.5"
serde = { version = "1.0", features = ["derive"] } 
toml = "0.9.8"
